const shoppingList = ['bread', 'butter', 'honey', 'coffee']
//pop removes last item
let lastItem = shoppingList.pop() //coffee
//shift removes first item
let firstItem = shoppingList.shift() //bread
console.log(shoppingList[0]) // butter
//neither pop nor shift accepts argument
